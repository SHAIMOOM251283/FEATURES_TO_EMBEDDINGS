# FEATURES_TO_EMBEDDINGS
A hands on comparison of machine learning and deep learning on one shared dataset. Book descriptions are scraped, then classified by genre three ways: hand engineered features, TF-IDF, and learned embeddings. Every model is scored on the same held out books, with an illustrated walkthrough of each.
